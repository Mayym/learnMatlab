% 复数的实部、虚部、模和幅角的计算
z=1+2*i;
real_z=real(z);       % 实部
imag_z=imag(z);       % 虚部
mag_z=abs(z);         % 模
angle_z0=angle(z);     % 幅角（弧度）
angle_z1=angle(z)*180/pi;       % 幅角（角度）
z
real_z
imag_z
mag_z
angle_z0
angle_z1